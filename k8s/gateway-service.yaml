apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: llm-platform
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
